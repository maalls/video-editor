{
  "name": "playground2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --out-dir dist",
    "build:watch": "babel src --out-dir dist --watch",
    "dev": "browser-sync start --server --files \"*.html, *.css, *.js, dist/*.js\"",
    "start": "browser-sync start --server --files \"*.html, *.css, *.js, dist/*.js\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.28.3",
    "@babel/core": "^7.28.4",
    "@babel/preset-env": "^7.28.3",
    "browser-sync": "^3.0.4"
  },
  "dependencies": {
    "marked": "^16.3.0"
  }
}
